name: tests

on: push

jobs:
  lint:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: install poetry
        run: pip install poetry

      - name: set up poetry
        run: make install

      - name: Run tests
        run: make test
